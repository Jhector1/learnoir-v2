import type { GenKey, TopicSlug } from "@/lib/practice/types";

/**
 * If you don’t have python generators yet, you can still keep this file.
 * Add GenKey(s) later (you'll likely expand GenKey union with "py_basics" etc).
 */
export const PY_GENKEY_TO_DB = {} as Record<GenKey, TopicSlug>;
